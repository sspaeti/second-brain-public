{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\" ?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq .Title .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{ . }} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne .Title .Site.Title }}{{ with .Title }}in {{ . }} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo</generator>
    <language>{{ .Site.Language.Locale }}</language>
    {{- if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end }}
    {{- $pctx := . }}
    {{- if .IsHome }}{{ $pctx = .Site.Home }}{{ end }}
    {{- with $pctx.OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{- /* Full-text feed of the last N changed notes, newest edit first —
       same "recently changed" ordering as the homepage's Recent Notes list
       (see layouts/partials/recent.html), so RSS matches what the site shows. */}}
    {{- $pages := .Pages }}
    {{- if .IsHome }}{{ $pages = .Site.RegularPages }}{{ end }}
    {{- $limit := .Site.Params.rssItems | default 50 }}
    {{ range first $limit $pages.ByLastmod.Reverse }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Lastmod.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <guid>{{ .Permalink }}</guid>
      {{- if .Site.Params.rssFullContent }}
      <description>{{ .Content | html }}</description>
      {{- else }}
      <description>{{ .Summary | html }}</description>
      {{- end }}
    </item>
    {{ end }}
  </channel>
</rss>
